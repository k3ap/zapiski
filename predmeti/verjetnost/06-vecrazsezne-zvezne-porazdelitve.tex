\podnaslov{Večrazsežne zvezne porazdelitve}

\begin{definicija}
  Slučajni vektor $\sv{X}$ ima \pojem{zvezno porazdelitev}, če obstaja
  nenegativna funkcija $f_{\sv{X}}(\sv{x})$, da za $A \subseteq \R^n$ velja
  \[
	P(\sv{X} \in A) = \int_A f_{\sv{X}}(\sv{x}) d\sv{x}.
  \]
  Funkciji $f_{\sv{X}}$ pravimo \pojem{gostota}.
\end{definicija}

\vprasanje{Definiraj gostoto porazdelitve slučajnega vektorja.}

\begin{izrek}
  Naj bo $f_{\sv{X}}(\sv{x})$ gostota vektorja $\sv{X}$ in $m < n$.
  Privzemimo, da je funkcija
  \[
	(x_1, \ldots, x_n) \mapsto \int_{\R^{n-m}} f_{\sv{X}}(x_1, \ldots, x_n)
	dx_{m+1} \ldots d_{x_n}
  \]
  Riemannovo integrabilna (lahko tudi v izlimitiranem smislu) po vseh Jordanovo
  izmerljivih množicah.
  Potem je to funkcija gostote vektorja $\sv{X}' = (x_1, \ldots, x_m)$.
\end{izrek}

\begin{izrek}
  Slučajna vektorja $\sv{X}, \sv{Y}$ sta neodvisna natanko tedaj, ko je
  \[
	f_{\sv{X}, \sv{Y}}(\sv{x}, \sv{y}) = f_{\sv{X}}(\sv{x}) f_{\sv{Y}}(\sv{y})
  \]
  skoraj povsod.
\end{izrek}

\begin{proof}
  V desno:
  Velja
  \begin{gather*}
	P(X \in A, Y \in B) = \int_{A \times B} f_{\sv{X}, \sv{Y}}(\sv{x}, \sv{y})
	d\sv{x} d\sv{y}, \\
	P(X \in A) P(Y \in B)
	= \int_A f_{\sv{X}}(\sv{x}) d\sv{x} \int_B f_{\sv{Y}}(\sv{y}) d\sv{y}
	= \int_{A \times B} f_{\sv{X}} f_{\sv{Y}}.
  \end{gather*}
  Ker sta vektorja neodvisna, sta ti količini enaki, torej sta integrirani
  funkciji enaki skoraj povsod.

  V levo:
  Velja
  \begin{align*}
	P(\sv{X} \in A, \sv{Y} \in B)
	&= \int_{A \times B} f_{\sv{X}}(\sv{x}) f_{\sv{Y}}(\sv{y}) d\sv{x} d\sv{y} \\
	&= \int_A f_{\sv{X}}(\sv{x}) d\sv{x} \int_B f_{\sv{Y}}(\sv{y}) d\sv{y} \\
	&= P(\sv{X} \in A) P(\sv{Y} \in B).
  \end{align*}
\end{proof}

\vprasanje{Karakteriziraj neodvisnost zvezno porazdeljenih slučajnih vektorjev
  in dokaži karakterizacijo.}

\begin{izrek}
  Naj bo $f_{\sv{X}, \sv{Y}}(\sv{x}, \sv{y}) = f(\sv{x}) g(\sv{y})$ za
  nenegativni $f, g$.
  Potem sta $\sv{X}$ in $\sv{Y}$ neodvisni.
\end{izrek}

Dokaz je praktično enak kot pri podobnem izreku za diskretne spremenljivke, le
da pišemo integrale namesto vsot.

\begin{izrek}
  Naj bo $\sv{X}$ slučajni vektor z gostoto $f_{\sv{X}}(\sv{x})$.
  Predpostavimo $P(\sv{X} \in U) = 1$ za neko odprto množico $U \subseteq \R^n$.
  Naj bo $\phi : U \to V$ difeomorfizem in $\sv{Y} = \phi(\sv{X})$.
  Velja $P(\sv{Y} \in V) = 1$ in
  \[
	f_{\sv{Y}}(\sv{y}) =
	\begin{cases}
	  f_{\sv{X}}\left( \phi^{-1}(\sv{y}) \right) \abs{ \det D\phi^{-1}(\sv{y}) }
	  & \sv{y} \in V \\
	  0 & \sv{y} \notin V
	\end{cases}
  \]
\end{izrek}

\vprasanje{Povej transformacijsko formulo.}

\begin{definicija}
  Naj bo $X$ zvezno porazdeljena slučajna spremenljivka z gostoto $f_X(x)$.
  Definiramo
  \begin{gather*}
	E(X) = \int_{-\infty}^\infty x f_X(x) dx, \\
	E(g(x)) = \int_{-\infty}^\infty g(x) f_X(x) dx.
  \end{gather*}
\end{definicija}

\vprasanje{Kako je definirana pričakovana vrednost zvezne slučajne
  spremenljivke?}

\begin{definicija}
  Za slučajno spremenljivko $X$ imenujemo količino $E(X^m)$ \pojem{$m$-ti
	moment}.
\end{definicija}

\begin{definicija}
  Za slučajno spremenljivko $X$ imenujemo količino $E( {(X - E(x))}^m )$
  \pojem{$m$-ti centralni moment}.
\end{definicija}

\vprasanje{Kaj je moment in kaj centralni moment slučajne spremenljivke?}

\begin{definicija}
  Če imamo množico števil $x_1, \ldots, x_n$, njihov \pojem{raztros} definiramo
  kot
  \[
	\sigma^2 = \inv{n} \sum_{k=1}^n (x_k - \bar{x})^2,
  \]
  kjer je $\bar{x}$ povprečje.
\end{definicija}

\begin{definicija}
  \pojem{Varianca} slučajne spremenljivke je količina
  \[
	\operatorname{var}(X) = E(X^2) - (E(X))^2.
  \]
  Pravimo, da varianca obstaja, če obstajata obe pričakovani vrednosti.
\end{definicija}

\begin{definicija}
  Naj bo $X$ slučajna spremenljivka, za katero $\operatorname{var}(X)$ obstaja.
  Količini $\sqrt{\operatorname{var}(X)}$ rečemo \pojem{standardni odklon}
  slučajne spremenljivke $X$ in jo označimo s $\operatorname{SD}(X)$.
\end{definicija}

\begin{definicija}
  \pojem{Kovarianca} dveh slučajnih spremenljivk je $\operatorname{cov}(X, Y) =
  E(XY) - E(X) E(Y)$.
  Rečemo, da obstaja, če obstajajo vse pričakovane vrednosti.
\end{definicija}

\vprasanje{Definiraj raztros, varianco, standardni odklon in kovarianco.}

\begin{izrek}
  Naj bodo $X_1, \ldots, X_m$ in $Y_1, \ldots, Y_n$ slučajne spremenljivke in
  $\alpha_1, \ldots, \alpha_m, \beta_1, \ldots, \beta_n$ konstante.
  Velja
  \[
	\operatorname{cov}\!\left( \sum_{k=1}^m \alpha_k X_k, \sum_{l=1}^n \beta_l
	  Y_l \right)
	= \sum_{k,l} \alpha_k \beta_l \operatorname{cov}(X_k, Y_l).
  \]
\end{izrek}

\begin{vo}{Povej nekaj lastnosti kovariance.}
  \begin{itemize}
  \item bilinearnost,
  \item $\operatorname{cov}(X,X) = \operatorname{var}(X)$,
  \item $\operatorname{cov}(X,Y) = \operatorname{cov}(Y,X)$,
  \item če sta $X$ in $Y$ neodvisni, je $\operatorname{cov}(X,Y) = 0$.
  \end{itemize}
\end{vo}

\begin{definicija}
  Količina
  \[
	\rho = \frac{\operatorname{cov}(X,Y)}{\sqrt{\operatorname{var}(X)
		\operatorname{var}(Y)}}
  \]
  se imenuje \pojem{korelacijski koeficient}.
\end{definicija}

\vprasanje{Definiraj korelacijski koeficient.}